[project]
name = "fdk-mpc-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.6.0",
    "pydantic>=2.11.2",
]

[dependency-groups]
dev = [
    "pyright>=1.1.398",
    "ruff>=0.11.3",
]
